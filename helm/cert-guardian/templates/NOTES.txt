Certificate Guardian has been deployed!

{{- if .Values.ingress.enabled }}
Access the application at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
To access the application, run:
  kubectl port-forward svc/{{ include "cert-guardian.fullname" . }}-frontend 3000:80 -n {{ .Release.Namespace }}
  Then open: http://localhost:3000
{{- end }}

Default login (local auth mode):
  Username: admin
  Password: admin (change immediately!)

{{- if .Values.metrics.enabled }}

Prometheus metrics available at:
  http://{{ include "cert-guardian.fullname" . }}-backend:8000/metrics
{{- if .Values.metrics.serviceMonitor.enabled }}
  ServiceMonitor is enabled - Prometheus will auto-discover metrics.
{{- end }}
{{- end }}

CORS: Set config.server.corsOrigins to your frontend URL in production.
  Default: ["*"] (allows all origins)

All data API endpoints require authentication (JWT token).

API documentation:
  kubectl port-forward svc/{{ include "cert-guardian.fullname" . }}-backend 8000:8000 -n {{ .Release.Namespace }}
  Then open: http://localhost:8000/docs
